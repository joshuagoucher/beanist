<div class="container">
<h3>Users Index</h3>
<ul class="list-group">
<% @users.each do |user| %>
	<li class="list-group-item">
		<%= user.name %> |
		<%= user.email %> |
		<%= link_to "Delete", user, method: :delete, data: {confirm: "Are you sure?"} %>
	</li>

<% end %>
</ul>

</div>