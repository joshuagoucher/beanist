
<% if signed_in? %>
	<% if current_user.admin? %>
		<strong>Admin User!</strong>
	<% end %>
	<strong class="welcome">Welcome, <%= current_user.name %></strong>
	<small class="signout">
		&#40;<%= link_to "Sign out", signout_path, method: :delete %>&#41;
	</small>
<% else %>
	<strong class="signin">
		<%= link_to "Sign in or Register", signin_path %>
	</strong>
<% end %>
